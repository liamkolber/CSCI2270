#include <iostream>
#include <vector>
#include <queue>

using namespace std;














/*int HashTable::hashSeed(string key) {
	int seed = 101;

	unsigned int hash = 0;

	for(int i = 0; i < key.length(); i++) {
		hash = (hash * seed) + key[i];
	}

	hash = hash % tableSize;
	return hash;

}*/